import { BuscadorTiendasPipe } from './buscador-tiendas.pipe';

describe('BuscadorTiendasPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorTiendasPipe();
    expect(pipe).toBeTruthy();
  });
});
